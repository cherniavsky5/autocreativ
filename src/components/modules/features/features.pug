include ../../widgets/card/card-feature/card-feature

mixin features(data)
  .features
    ._grid
      each item in data
        ._column(class=(item.img && 'features__column--photo'))
          +card-feature(item)